class Book {
    constructor(id, name, author, description, publisher) {
        this.id = id,
        this.name = name,
        this.author = author,
        this.description = description,
        this.publisher = publisher
    }
}

module.exports = Book;